Common tests for unsafe-nulls feature.

All the tests should be compiled with `-language:unsafeNulls`.

All the tests should produce errors at `// error` comment locations without `-language:unsafeNulls`.
