package example

class `local-file/*<<=example.`local-file`#*/` {
  locally/*=>>dotty.DottyPredef.locally().*/ {
    val local/*<<=local0*/ = 42
    local/*=>>local0*/ +/*=>>scala.Int#`+`(+4).*/ 4
  }
}
