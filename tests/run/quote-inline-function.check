Normal function
{
  var i: Int = 0
  val j: Int = 5
  <label> def while$(): Unit = 
    if i.<(j) then 
      {
        val x$1: Int = i
        f.apply(x$1)
        i = i.+(1)
        while$()
      }
     else ()
  while$()
}

By name function
{
  var i: Int = 0
  val j: Int = 5
  <label> def while$(): Unit = 
    if i.<(j) then 
      {
        val x$1: Int = i
        scala.Predef.println(x$1)
        i = i.+(1)
        while$()
      }
     else ()
  while$()
}
